- name: Ensure user bob is present in OU ou=test,dc=domain,dc=local
  microsoft.ad.user:
    identity: bob
    password: B0bP4ssw0rd
    state: present
    path: OU=Holding,DC=mvega,DC=local
    groups:
      set:
        - Domain Admins
        - Domain Users
