# Name the file microsoft.ad.ldap.yml

plgin: microsoft.ad.ldap

### Supplied by custom credential type ###
# server: 
# port:
# username:
# password:
### End custom credential type ###

search_base: OU=Domain Controlllers,DC=mvega,DC=local

# see plugin documentation for additional configuration properties for filtering, grouping, etc.u
